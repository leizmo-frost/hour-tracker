<div>
    <div class="page-title"><div><h1>PTO / Leave</h1><p>Submit time-off requests and track their approval status.</p></div></div>
    @if($message)<div class="notice {{ $messageType }}">{{ $message }}</div>@endif
    <div class="grid grid-2">
        <div class="panel panel-pad"><div class="toolbar" style="padding:0 0 16px;border:0"><div><h2>New request</h2><div class="muted">Requests use an eight-hour workday.</div></div></div><div class="form-grid"><div class="field"><label>Type</label><select class="select" wire:model="type"><option value="vacation">Vacation</option><option value="sick">Sick</option><option value="personal">Personal</option><option value="other">Other</option></select></div><div></div><div class="field"><label>Start date</label><input class="input" type="date" wire:model="startsOn">@error('startsOn')<small class="error">{{ $message }}</small>@enderror</div><div class="field"><label>End date</label><input class="input" type="date" wire:model="endsOn">@error('endsOn')<small class="error">{{ $message }}</small>@enderror</div><div class="field full"><label>Reason</label><textarea class="textarea" wire:model="reason"></textarea></div></div><button wire:click="submit" class="btn btn-primary">Submit request</button></div>
        <div class="panel"><div class="toolbar"><div><h2>Your requests</h2><div class="muted">Most recent first</div></div></div><div class="table-wrap"><table><thead><tr><th>Dates</th><th>Type</th><th>Hours</th><th>Status</th></tr></thead><tbody>@forelse($requests as $request)<tr><td>{{ $request->starts_on->format('d M') }} – {{ $request->ends_on->format('d M Y') }}</td><td>{{ ucfirst($request->type) }}</td><td>{{ number_format($request->hours,2) }}h</td><td><span class="badge {{ $request->status==='approved'?'badge-green':($request->status==='rejected'?'badge-red':'badge-yellow') }}">{{ $request->status }}</span></td></tr>@empty<tr><td colspan="4"><div class="empty">No leave requests yet.</div></td></tr>@endforelse</tbody></table></div></div>
    </div>
</div>
